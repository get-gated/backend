export enum SentReceivedStatType {
  Domain = 'domain',
  Address = 'address',
}

export enum SentReceivedStatSortBy {
  ReceivedCount = 'receivedCount',
  SendCount = 'sentCount',
  LastSeenAt = 'lastSeenAt',
  firstSeenAt = 'firstSeenAt',
}
